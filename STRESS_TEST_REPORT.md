# 🔬 강력 스트레스 테스트 보고서 (STRESS_TEST_REPORT)

> 검증 코드: `scripts/checkIntegrity.ts` (실행: `npx tsx scripts/checkIntegrity.ts`)
> 범례: 🔴 치명(수정) · 🟡 중간(수정) · 🟢 경미(보고만)
> 진행: **A·B 완료** / C~F 예정

---

## ✅ Test A — 데이터 / 마이그레이션

저장 스키마 진화에 따른 구버전 저장본 보정·기본값을 검증. (`migrateLoadedGame`를 순수 함수로 추출해 IndexedDB 없이 테스트)

| # | 검사 | 결과 |
|---|---|---|
| A1 | 기본 상태: **BGM 기본 OFF**, 효과음·음성 기본 ON | ✅ |
| A2 | 빈 저장본 → 모든 필드 채워짐, BGM OFF 적용 | ✅ |
| A3 | 구버전(필드 누락) 저장본 → 기존 값 보존 + 8감정/7도장/v4 필드 보정 + BGM 1회 OFF | ✅ |
| A4 | **BGM 켠 사용자**는 마이그레이션이 재-음소거하지 않음(토글 무력화 방지) | ✅ |
| A5 | `emptyEmotionCounts` 8감정 · `emptyDojoProgress` 7도장 완전 | ✅ |
| A6 | 성장 필드 없는 구버전 `GardenEntry` → stage='bloom', health='healthy', 예외 없음 | ✅ |

→ **마이그레이션 문제 0건.** 구버전 사용자 데이터 유실·크래시 없음.

## ✅ Test B — 정합성 (끊긴 참조)

데이터 모듈을 가로질러 교차 참조의 무결성을 검증.

| # | 검사 | 결과 |
|---|---|---|
| B1 | 식물 종(60) ↔ SVG 컴포넌트(60) 일치 | ✅ |
| B2 | 음성 대본 389개 id **유일** + character 모두 `VOICE_CHARACTERS`에 존재 | ✅ |
| B3 | 모든 미션 안내 음성 클립 대본 존재(코드가 부르는 `mission_*` 전부) | ✅ |
| B4 | 멘토 7명 정답/오답 반응 클립 존재 | ✅ |
| B5 | 식물 대화 8감정 + 성장 클립 존재 | ✅ |
| B6 | 위인 15명 한마디 클립 존재 | ✅ |
| B7 | 미션 63개 구조: answer 인덱스 범위, needCorrect, **dance 시퀀스↔moves**, card_order 순열, id 유일 | ✅ |
| B8 | 도장 7개: 멘토 아바타·음성·영웅·건물 참조 | ✅ (수정 후) |
| B9 | 위인 15명 ↔ 에피소드 참조 + id 유일 | ✅ |

---

## 🟡 발견 및 수정 (중간 1건)

### 🟡 B8-1 · 도장 `finalReward.buildingId` 죽은 참조 (7건) — **수정함**
- **증상**: 7개 도장 모두 `finalReward.buildingId`가 `'music_dojo'`·`'pe_dojo'` 등 **`BUILDINGS`에 존재하지 않는 id**를 가리킴.
- **영향 분석**: `finalReward` 사용처는 `.heroId`·`.titleName`뿐이고 **`.buildingId`는 어떤 코드도 읽지 않음**(검증 완료). 런타임 크래시·기능 손상 **없음** → 잠재적 끊긴 참조(향후 foot-gun).
- **분류 근거**: 무해하지만 "존재하지 않는 객체를 가리키는 참조"이므로 🟢가 아닌 🟡로 판단.
- **수정**: `Dojo.finalReward.buildingId`를 **optional**로 바꾸고, 7개 도장의 잘못된 값 제거. (도장→왕국 건물 연출은 애초에 미구현 기능 — 데이터만 정리)
- **수정 후**: 재검증 0건.

> 참고: 왕국 건물(`BUILDINGS` 15개)은 **법교육 에피소드(ep01~15)** 전용이며 정상 작동. 도장 사범 보상은 영웅카드 + 칭호로 이미 완결.

## 🔴 치명 / 🟢 경미
- 🔴 치명: **0건**
- 🟢 경미: **0건**

---

---

## ✅ Test C — 경계값 / 극단입력 (`scripts/checkRuntime.ts`)

| # | 검사 | 결과 |
|---|---|---|
| C1 | 수학 문제 **약 16,800개 무작위** 생성: 정답∈보기, 보기 유일·≥2개·음수 없음, **표시 수식과 정답 일치**, 나눗셈 정수 | ✅ |
| C2 | 성장 극단 시간(10년 뒤 → fruit에서 정지, 음수 경과 → 미성장) | ✅ |
| C3 | 정원 레벨 XP 경계(-99999 / 0 / 9,999,999) | 🟡→수정 |
| C4 | 돌봄 쿨다운 경계(미래 시각 lastCareAt, 음수 방지) | ✅ |
| C5 | 음성 텍스트 다듬기 극단입력(빈문자열·기호·이모지·5000자) | ✅ |

## ✅ Test D — 시간 / 상태

| # | 검사 | 결과 |
|---|---|---|
| D1 | 날짜 차이: 같은날 0, 연말→연초 1, 역순 -1 | ✅ |
| D2 | 오늘 날씨 하루 동안 결정적(고정) + 유효 타입 | ✅ |
| D3 | 건강 계산: 미래/과거 lastWatered, 구버전(물준적 없음) — **죽음 없음** | ✅ |
| D4 | 학년별 개념: 미설정·0·음수·초과 학년 모두 유효 목록 | ✅ |

## ✅ Test E — 오디오

| # | 검사 | 결과 |
|---|---|---|
| E1 | **AudioContext 없는 환경**(SSR/구형 브라우저 모사)에서 모든 음 함수 예외 없이 no-op | ✅ |
| E2 | 음감 미션 계이름(도~도') 8개 ↔ 주파수 매핑 완전 | ✅ |
| (검토) | `voice.ts` 404 폴백 · `music.ts` Tone.start 실패 catch · `sound.ts` onloaderror · `audio.ts` ctx null 가드 — 모두 graceful | ✅ |

## ✅ Test F — 입력검증

| # | 검사 | 결과 |
|---|---|---|
| F1 | 이름: 1~10자 + 공백 차단(`NameInputScreen`) | ✅ |
| F2 | 부모 PIN: 숫자만·4자리(`ParentReportScreen`) / 별명: 8자 제한 | ✅ |
| F3 | `voiceCleanPrompt` 극단입력 새니타이즈(C5와 동일) | ✅ |
| F4 | `importData`: 잘못된 JSON → try/catch로 안전하게 false | ✅ |

### 🟡 발견 및 수정 (중간 1건)
**🟡 C3-1 · `levelProgress` 진행률 범위 이탈 — 수정함**
- **증상**: 음수 XP(`levelProgress(-100)`)에서 `ratio = -1.67`로 [0,1]을 벗어남 → 진행바 폭이 음수가 될 수 있음.
- **영향**: `gardenXp`는 0부터만 증가하므로 정상 흐름에선 도달 불가하나, 진행바 렌더 안전을 위한 방어 부재.
- **수정**: `ratio`를 `Math.max(0, Math.min(1, …))`로 클램프 + `toNext` 음수 방지.

### 🟢 관찰(보고만, 미수정)
- 🟢 F4-a: `importData`가 프로필 저장 후 엔트리에서 실패하면 부분 임포트 가능(현재 try/catch로 false 반환·UI 비노출). 정상 데이터에선 도달 안 함.
- 🟢 C-a: `spendEmpathy`/`awardEmpathy`에 음수 인자 방어 없음(모든 호출부가 양수 비용 전달 — 도달 불가).

---

## 📊 종합 요약 (A~F)
| 테스트 | 검사 항목 | 🔴 | 🟡 | 🟢 |
|---|---|---|---|---|
| A 마이그레이션 | 6 | 0 | 0 | 0 |
| B 정합성 | 9 | 0 | 1(수정) | 0 |
| C 경계값/극단 | 5 | 0 | 1(수정) | 1 |
| D 시간/상태 | 4 | 0 | 0 | 0 |
| E 오디오 | 2+ | 0 | 0 | 0 |
| F 입력검증 | 4 | 0 | 0 | 1 |
| **합계** | **30+** | **0** | **2(수정)** | **2(보고)** |

**최종 결론**: 6개 영역 전수 스트레스 테스트 결과 **🔴 치명 0건**. 발견된 🟡 2건(도장 죽은 참조, 진행률 범위)은 모두 수정 완료. 🟢 2건은 현재 도달 불가하여 보고만. 검증 코드 2종(`checkIntegrity.ts`·`checkRuntime.ts`)은 회귀 방지용으로 보존. tsc 0, build SUCCESS.
